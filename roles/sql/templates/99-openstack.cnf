[mysqld]
bind-address = {{ group_vars.sql.bind_address }}

default-storage-engine = innodb
innodb_file_per_table = on
max_connections = {{ group_vars.sql.max_connections }}
collation-server = utf8_general_ci
character-set-server = utf8
