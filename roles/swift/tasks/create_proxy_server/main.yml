---
- name: Configure swift-proxy-server
  ansible.builtin.template:
    src: etc/swift/proxy-server.conf
    dest: /etc/swift/proxy-server.conf
    owner: swift
    group: swift
    mode: 0644
    backup: yes
  register: template_result

